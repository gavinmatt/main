---
import BaseLayout from "../layouts/BaseLayout.astro";
import FlightData from "../components/FlightData"; // Import the FlightData component

const { title = 'Flight Tracker', description = 'Track flight data' } = Astro.props;
---

<BaseLayout title={title} description={description}>
    <FlightData client:load /> <!-- Render the FlightData component -->
</BaseLayout>
